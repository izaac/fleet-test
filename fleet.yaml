defaultNamespace: cert-manager
diff:
  comparePatches:
  # Ignored in Fleet, since it causes a "modified" state. See https://fleet.rancher.io/bundle-diffs/
  - apiVersion: cert-manager.io/v1
    kind: ClusterIssuer
    name: vault-issuer
    operations:
    - {"op":"remove", "path":"/spec/vault/server"}
